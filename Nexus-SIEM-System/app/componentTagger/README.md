# Component Tagger

A utility tool for identifying and tagging UI components in the codebase.

## Purpose

Used by the build system to analyze component usage and inject metadata for development tools.

## Files

- **src/component-tagger.ts**: Main logic for component tagging.
- **config/component-config.json**: Configuration settings for the tagger.
